using UnityEngine;
using UnityEngine.UIElements;

public class ReadMeStreetNodes : MonoBehaviour
{
    //The point of these nodes is that they are mostly straight, with a few 'intersections'
    //Then I can code it so that the tiles generate buildings along the edges
}
